// next.config.js alebo next.config.ts
/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['en', 'sk'],
    defaultLocale: 'en',
  },
};

module.exports = nextConfig;